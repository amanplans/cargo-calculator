export class Weight {
    originalValue: string;
    value: number;

    constructor(originalValue: string, value: number) {
        this.originalValue = originalValue;
        this.value = value;
    }
}
